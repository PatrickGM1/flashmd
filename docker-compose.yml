services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - deck-data:/app/data
    expose:
      - "8080"

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"

volumes:
  deck-data:
